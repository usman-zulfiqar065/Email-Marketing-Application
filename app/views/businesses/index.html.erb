<div class="bg-body-secondary p-3 d-flex flex-wrap justify-content-between">
  <div class="fs-4 fw-semibold">Manage Your Bussiness and Leads</div>
  <div><%= link_to "New business", new_business_path %></div>
</div>

<div class="d-flex flex-wrap p-5 justify-content-center">
  <% @businesses.each do |business| %>
    <div class="border border-1">
      <div class="fs-5 fw-semibold bg-body-secondary p-4"><%= business.name %></div>
      <div class="p-4"> <%= link_to "Show this business", business %> </div>
    </div>
  <% end %>
</div>
